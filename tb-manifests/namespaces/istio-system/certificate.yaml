apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: selfsigned-cert
  namespace: istio-system
spec:
  dnsNames:
    - eagle-console.tranquilitybase-demo.io
  secretName: ec-tls-credential
  issuerRef:
    name: tb-selfsigned
